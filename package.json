{
  "name": "report-bot",
  "version": "1.0.0",
  "repository": "git@github.com:vacuumlabs/report-bot.git",
  "author": "Tomas Fabry <tomas.fabry@vacuumlabs.com>",
  "license": "Unlicense",
  "scripts": {
    "knex": "knex --knexfile knex/knexfile.js",
    "build": "yarn --cwd client && yarn --cwd client build && yarn --cwd server && yarn --cwd server build",
    "clean": "rimraf node_modules && yarn --cwd client clean && yarn --cwd server clean",
    "client": "yarn --cwd client start",
    "server": "yarn --cwd server dev",
    "start": "yarn --cwd server start",
    "dev": "concurrently \"yarn server\" \"yarn client\"",
    "eslint": "(cd server; yarn eslint) && (cd bot; yarn eslint;) && (cd client; yarn eslint)",
    "test": "yarn eslint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn test"
    }
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "knex": "^0.18.1",
    "pg": "^7.11.0",
    "transenv": "^1.0.3"
  }
}
